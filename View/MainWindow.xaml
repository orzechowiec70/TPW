<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Symulator Bilarda" Height="550" Width="850">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="10">
            <Label Content="Liczba kul:" />
            <TextBox Width="50" Text="{Binding NumberOfBalls, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" />
            <Button Content="Start" Command="{Binding StartCommand}" Margin="10,0,0,0" Width="80" />
            <Button Content="Stop" Command="{Binding StopCommand}" Margin="10,0,0,0" Width="80" />
        </StackPanel>

        <Viewbox Grid.Row="1" Stretch="Uniform" Margin="10">
            <ItemsControl ItemsSource="{Binding Balls}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Background="DarkGreen" Width="800" Height="400" IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Fill="Red" 
                                 Width="{Binding Diameter}" 
                                 Height="{Binding Diameter}"
                                 Margin="-15,-15,0,0"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Viewbox>
    </Grid>
</Window>